[package]
name = "transformer"
version = "0.1.0"
edition = "2021"
license = "LicenseRef-RunuX-Commercial"
description = "Transformer inference engine for RunuX AI Runtime"

[dependencies]
ai_runtime = { path = "../ai_runtime" }
rvv_simd = { path = "../rvv_simd" }
turbo_quant = { path = "../turbo_quant" }
