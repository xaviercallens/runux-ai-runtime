[package]
name = "perf_model"
version = "0.1.0"
edition = "2021"
license = "LicenseRef-RunuX-Commercial"
description = "Analytical performance model for RISC-V LLM inference (RunuX AI Runtime)"

[dependencies]
ai_runtime = { path = "../ai_runtime" }
